# Backtest/Execution engine v2 (paper)

- Engine event-driven theo bar với chi phí/slippage tường minh.
- Hỗ trợ `long_only` và `long_short` (crypto perp paper).
- Báo cáo có `config_hash`, `dataset_hash`, `code_hash`, `report_id` để tái lập.
- Đây là mô phỏng giáo dục, không cam kết lợi nhuận.
